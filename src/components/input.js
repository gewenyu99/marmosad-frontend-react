import styled from "styled-components";

export const Input = styled.input`
  padding: 0.5em;
  margin: 0.5em;
  color: ${props => props.color};
  background: white;
  border: none;
  border-radius: 3px;
`;
